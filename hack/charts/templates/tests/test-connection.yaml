apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nuwa.fullname" . }}-test-connection"
  labels:
    {{- include "nuwa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nuwa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
