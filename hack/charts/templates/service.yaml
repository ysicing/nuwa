apiVersion: v1
kind: Service
metadata:
  name: {{ include "nuwa.fullname" . }}
  labels:
    {{- include "nuwa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nuwa.selectorLabels" . | nindent 4 }}
